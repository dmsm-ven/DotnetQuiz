<UserControl x:Class="DotnetQuiz.Views.QuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <TextBlock Text="Загрузка вопросов..."
                   FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <Grid Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" FontSize="16" Margin="0,0,0,16">
                <Run Text="Вопрос "/><Run Text="{Binding CurrentQuestionNumber, Mode=OneWay}"/><Run Text=" из "/><Run Text="{Binding TotalQuestions, Mode=OneWay}"/>
            </TextBlock>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <TextBlock Text="{Binding CurrentQuestion.Text}"
                           FontSize="20" FontWeight="Bold" TextWrapping="Wrap"
                           Margin="0,0,0,24"/>

                <RadioButton Content="{Binding CurrentQuestion.OptionA}" GroupName="Options"
                             IsChecked="{Binding SelectedOptionIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=0}"/>
                <RadioButton Content="{Binding CurrentQuestion.OptionB}" GroupName="Options"
                             IsChecked="{Binding SelectedOptionIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=1}"/>
                <RadioButton Content="{Binding CurrentQuestion.OptionC}" GroupName="Options"
                             IsChecked="{Binding SelectedOptionIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=2}"/>
                <RadioButton Content="{Binding CurrentQuestion.OptionD}" GroupName="Options"
                             IsChecked="{Binding SelectedOptionIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=3}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Закончить" Command="{Binding FinishCommand}" Width="130" Margin="0,0,12,0"/>
                <Button Content="Далее" Command="{Binding NextCommand}" Width="130"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
